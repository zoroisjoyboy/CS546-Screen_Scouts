<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>"Sign In Page"</title>
    <link rel="stylesheet" href="/public/css/main.css">
</head>

<body>
    <div id="signinView">
        <h1>Sign In</h1>

        {{#if error}}
            <p id="errorMessage" style="color: red;">{{error}}</p>
        {{/if}}

        <form id="signin-form" method="POST" action="/signinuser">
            <div>
            <label for="userName">User ID:</label>
            <input type="text" id="userName" name="userName">
            </div>
            <div>
            <label for="password">Password:</label>
            <input type="password" id="password" name="password">
            </div>
            <div>
            <button type="submit" id="submit_button" name="submit_button">Sign In</button>
            </div>
        </form>

        <p>Don't have an account? <a href="/signupuser">Click here to signup!</a></p>
    </div>

</body>
</html>
